{
  "name": "infinitee",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd bot && NODE_ENV=development npm ci && npm run build",
    "start": "cd bot && node dist/index.mjs"
  }
}
